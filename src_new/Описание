Что сделать

1. Смотрим в npm view <packageName> --dependencies (проверить синтаксис), также кешируя эти данные
2. Строим коллекцию List, в которой будут все пакеты зависимостей из 1 пункта
3. Кешируем данные об датах пакетов используя npm view <packageName> time
4. Для каждого пакета из коллекции List мы смотрим, каких его зависимостей есть или что есть в нексусе
Подробнее:
    а. Если зависимость ^3.3.3, а в нексусе есть [4.0.0, 3.1.1], значит, мы добавляем ВО ВРЕМЕННУЮ КОЛЛЕКЦИЮ [versionOfType] зависимость 3.3.3'
    б. Повторить для всех пакетов
    б. Мы сформируем временную коллекцию [versionOfType], например в ней будет что-то типа [3.3.3, 3.4.4, 3.4.5], но версия 3.4.5 покрывает все остальные зависимости, и мы добавляем
    именно ее


Шаги:
1. Сделать функцию npmViewDependencies
2. Кешировать результат выполнения функции npmViewDependencies в отдельный JSON

TODO дописать
